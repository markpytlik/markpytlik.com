# markpytlik.com — public homepage, crawlable.
#
# The private work area is kept out of search: work.html and every case page
# carry <meta name="robots" content="noindex, nofollow">, and the gate script +
# case copy (/js/) and case/reel media (/assets/work/) are blocked from crawling
# so none of it can surface in web or image search. robots.txt only guides
# crawlers — it is not access control (see js/work.js).

User-agent: *
Disallow: /js/
Disallow: /assets/work/
Disallow: /docs/
Disallow: /tools/
