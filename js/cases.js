/* ============================================================
   Case study content — TEMPORARILY REMOVED.

   The case-study data (copy, media URLs, captions) and the work.js access
   codes were pulled out of the public source while a real, server-side gate
   is built (Cloudflare Access / Pages Functions + a datastore).

   The previous content is preserved in git history — restore it from the
   commit before this one when wiring up the Cloudflare version.
   ============================================================ */
window.MP_CASES = {};
